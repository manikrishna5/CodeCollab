const StatusBar = () => {
  return (
    <footer className="status-bar">
      <span>JavaScript</span>

      <span>Connected</span>

      <span>Saved</span>
    </footer>
  );
};

export default StatusBar;